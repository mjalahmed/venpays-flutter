name: venpays_flutter
description: >-
  VenPays Flutter SDK for hosted Mastercard card checkout on Android and iOS.
  Opens VenPays hosted payment in a WebView; no card data or API secrets in the app.
version: 0.1.2
homepage: https://venpays.com
repository: https://github.com/mjalahmed/venpays-flutter

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
